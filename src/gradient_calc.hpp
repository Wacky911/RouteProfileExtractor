#ifndef GRADIENT_CALC_HPP
#define GRADIENT_CALC_HPP

#include <iostream>
#include <vector>
#include <cmath>

std::vector<double> gradientCalc(const std::vector<double>& elevation, const std::vector<double>& distance);

#endif // GRADIENT_CALC_HPP
